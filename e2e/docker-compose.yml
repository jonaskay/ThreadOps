services:
  pubsub:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    command: gcloud beta emulators pubsub start --project=threadops-test --host-port=0.0.0.0:8085
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8085:8085"
